/**
 *    mvc
 *        model（模型）是应用程序中用于处理应用程序数据逻辑的部分，通常模型对象负责在数据库中存取数据。
 *        view（视图）是应用程序中处理数据显示的部分，通常视图是依据数据模型创建的。视图层，前端
 *        Controller（控制器）是应用程序中处理用户交互的部分。一般包括业务处理模块和 router路由模块
 *                   通常控制器负责从视图读取数据，控制用户输入，并向模型发送数据
 * 
 * 
 *    mvvm 
 *        mvvm是前端图层概念，mvvm是双向的，视图层可以通过viewmodel转换成模型层，模型层也可以通过 viewmodel转换成视图层，
 *          model（每个页面单独的数据）
 *          viewmodel （双向绑定，m 和 v 之间的枢纽）
 *          view（视图，相当于 html 结构）
 * 
 * 
 */ 