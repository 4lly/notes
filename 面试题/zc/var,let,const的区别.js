/*  var 
 *  var 声明的变量可以提升 let const 不会  
 *  var 可以重复声明 
 *  var 在非函数作用域中定义是挂载到window 上的 
 * 
 *  let 
 *  let 声明块级作用域只在局部起作用
 *  let 防止变量污染
 *  let 不可再声明 
 * 
 *  const 
 *  具备 let的所有特征 
 *  不可被改变 
 *    const 不可被改变的是直接的地址，如果 const 声明的是对象的话，可以修改对象里面的值   
 * 
 */